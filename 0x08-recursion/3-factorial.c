#include "main.h"

/**
 * factorial - returns the factorial of a given number
 * @n: integer
 * Return: int
*/

int factorial(int n)
{
	if (n == 1)
	{
		return 1;
	}
	else if
	{
		return (n * factorial(n - 1));
	}
}
